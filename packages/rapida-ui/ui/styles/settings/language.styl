@import url('https://fonts.googleapis.com/css?family=Muli:600,700')

font= {
  family: 'Muli',
  stack: font-stack('Helvetica'),
  weight: 600,
  size: 14px
}

dir= {
  main: ltr,
  side: rtl
}
align= {
  main: left,
  side: right
}
